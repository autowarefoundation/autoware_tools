<launch>
  <arg name="input_pcd_or_dir" default="" description="Path to the folder containing the input PCD files"/>
  <arg name="output_pcd_dir" default="" description="Path to the folder containing the segmented PCD files"/>
  <arg name="prefix" default="" description="Prefix for the name of the output PCD files"/>
  <arg name="config_file" default="$(find-pkg-share autoware_pointcloud_divider)/config/pointcloud_divider.param.yaml" description="Path to the configuration YAML file"/>

  <group>
    <node pkg="autoware_pointcloud_divider" exec="autoware_pointcloud_divider_node" name="pointcloud_divider" output="screen">
      <param name="input_pcd_or_dir" value="$(var input_pcd_or_dir)"/>
      <param name="output_pcd_dir" value="$(var output_pcd_dir)"/>
      <param name="prefix" value="$(var prefix)"/>
      <param name="config_file" value="$(var config_file)"/>
    </node>
  </group>
</launch>
